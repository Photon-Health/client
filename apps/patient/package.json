{
  "name": "patient",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest",
    "pullenv": "aws sso login && secret=$(aws secretsmanager get-secret-value --secret-id patient_build_vars --query SecretString --output text) && echo \"$secret\" | jq -r 'to_entries[] | \"\\(.key)=\\(.value)\"' > .env && echo \"\nSuccessfully downloaded .env file\""
  },
  "dependencies": {
    "@rudderstack/analytics-js": "^3.22.0",
    "chakra-react-select": "^4.6.0",
    "dayjs": "^1.11.7",
    "react-helmet": "^6.1.0",
    "tinycolor2": "^1.6.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
