{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "pullenv": "secret=$(aws secretsmanager get-secret-value --secret-id app_build_vars --query SecretString --output text) && echo \"$secret\" | jq -r 'to_entries[] | \"\\(.key)=\\(.value)\"' > .env"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "jest": {
    "transformIgnorePatterns": [
      "/node_modules/@photonhealth/react"
    ]
  },
  "dependencies": {
    "@photonhealth/elements": "*",
    "@photonhealth/react": "*",
    "@photonhealth/sdk": "*"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
